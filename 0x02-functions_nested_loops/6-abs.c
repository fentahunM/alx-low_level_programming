#include "main.h"
/**
 *_abs - computes the absolute value of an integer
 *@n: the value to be checked
 *
 *Return: 0
 */
int _abs(int n)
{
	if (n < 0)
	{
		n = (-1 * n);
		return (n);
	}
	else
	{
		return (n);
	}
}
